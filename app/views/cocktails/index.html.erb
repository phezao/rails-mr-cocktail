<h1>Cocktails</h1>

<p>All the cocktails available</p>

<ul>
  <% @cocktails.each do |t| %>
    <li><%= link_to t.name , cocktail_path(t) %></li>
  <% end %>
</ul>

<%= link_to 'Create a new cocktail', new_cocktail_path, class: 'btn btn-primary' %>
